export async function sendNotification(userId: string, message: string): Promise<void> {
  // Replace with actual notification logic when available
  console.log(`Sending notification to user ${userId}: ${message}`);
}
